{
  "name": "autodetect-pdf-fields-server",
  "version": "0.1.0",
  "description": "HTTP API server for autodetect-pdf-fields",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch run index.ts"
  },
  "dependencies": {
    "@orpc/openapi": "^1.13.13",
    "@orpc/server": "^1.13.13",
    "@orpc/zod": "^1.13.13",
    "zod": "3"
  }
}
